import axiosInstance from './axiosInstance';
import { ENDPOINTS } from '../config/environment';

// Nota: Los endpoints de notificaciones no están implementados en el backend aún
// Estos son placeholders para cuando se implemente

/*
export const getNotifications = (userId) =>
  axiosInstance.get(ENDPOINTS.NOTIFICATIONS.GET(userId));

export const getUnreadCount = (userId) =>
  axiosInstance.get(ENDPOINTS.NOTIFICATIONS.GET_UNREAD_COUNT(userId));

export const markAsRead = (notificationId) =>
  axiosInstance.put(ENDPOINTS.NOTIFICATIONS.MARK_AS_READ(notificationId));
*/
